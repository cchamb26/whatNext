import { foodEntry } from "../../packages/backend/types";

export async function getRecentFoodEntries(limit: number = 30 ): Promise<foodEntry[]>{
    //once DB is setup, finalize method
    //returns a list of <=30 entries with foodEvents.
    return[];
}